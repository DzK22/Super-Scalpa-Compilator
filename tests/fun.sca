(* Programme qui effectue des tests basiques avec des fonctions *)

program Fun
    var a : int;

    function toto (b : int, c : int) : unit
    begin
      write " b=";
      write b;
      write ", c=";
      write c;
    end;

    begin
        toto(1, 2);
        toto(3, 4);
        write "\n"
    end
