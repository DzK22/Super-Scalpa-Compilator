(* Programme qui effectue des tests basiques avec des fonctions *)

program Fun
    var a : int;

    function toto (b : int, c : bool, d : int) : unit
    begin
      write " b=";
      write b;
      write ", c=";
      write c;
      write ", d=";
      write d;
    end;

    begin
        toto(1, true, 22);
        toto(3, false, 15);
        write "\n"
    end
