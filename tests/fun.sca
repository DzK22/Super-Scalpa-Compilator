(* Programme qui effectue des tests basiques avec des fonctions *)

program Fun
    var a : int;

    function toto (b : int) : unit
    begin
        a := a + 2;
    end;

    begin
        toto(1);
        toto(2);
        write a;
    end
