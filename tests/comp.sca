program comp
  var a, c, t : int;
  var test : bool;
    begin
        t := 8 ;
        a := 5 ;
        test := true;
        write -1 ;
        if (test and t < a) then write "it works guys without brackets" else write "tata";
    end
