program tabz
  var a, c, t : int;
  var tab : array[-4..4,1..3] of int ;
    begin
        a := tab[-3,2];
        write a;
        write "\n";
    (*  c := tab[2,2];
        write c;
        write "\n";
        t := tab[3,3];
        write t;
        write "\n";*)
     end
