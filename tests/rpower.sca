program rpower
var i, j : int;
function power (x : int, n : int) : int
begin
    if n = 0 then return 1
        else return (x * power(x, n - 1));
end;
begin
    i := 2;
    j := 10;
    write power(i, j);
    write "\n";
end
