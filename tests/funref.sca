program Funref
    var a : int;

    function test (ref t : int) : unit
    begin
        t := t + 2 * 5;
    end;

    begin
        test(a);
        write a; (* a doit valoir 2 *)
        write "\n";
    end
