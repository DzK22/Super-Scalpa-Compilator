(* ça marche pas putaing *)

program fibo
    var d : int;
    function fib (n : int) : int
    begin
        if (n = 0 or n = 1) then return 1;

        (* n >= 2 *)
        return fib(n - 1) + fib(n - 2);
    end;

    begin
        d := 2;
        write "fib(";
        write d;
        write ") = ";
        write fib(d);
        write "\n";
    end
