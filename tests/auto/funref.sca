program funref
    var a : int;

    function test (ref t : int) : unit
    begin
        t := t + 2 * 5;
    end;

    begin

        a := -2 ;

        test(a);
        write a;
        write "\n";

        test(a);
        write a;
        write "\n";


    end
