program fun2
    var d : int;

    function f1 (k : int) : int
    begin
        return k * 2;
    end;

    function f2 (l : int) : int
    var d : int;
    begin
        d := -10;
        return f1(l) + 2 + d;
    end;

    begin
        write "f2(";
        write d;
        write ") = ";
        write f2(d);
        write "\n";

        d := -3;

        write "f2(";
        write d;
        write ") = ";
        write f2(d);
        write "\n";
    end
